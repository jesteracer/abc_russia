jQuery ->
  # $("form.new_photo .button").live "click", ->
  #   alert "cl"
  # console.log $(".button")
  # $("#new_photo").bind "ajax:complete", (e, data, status, error) ->
  #   photo = data.responseJSON.photo
  #   thumb_url = photo.thumb_url
  #   $("#person_photos .thumbs").append("<img src='#{thumb_url}'>")

  # $("#new_photo .button").bind "click", ->
  #   $("#new_photo #photo_attachment").click()

  # $("#new_photo #photo_attachment").bind "change", ->
  #   $("#new_photo").submit()